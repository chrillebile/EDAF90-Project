import { MdbTableSortDirective } from './mdb-table-sort.directive';

describe('MdbTableSortDirective', () => {
  it('should create an instance', () => {
    const directive = new MdbTableSortDirective();
    expect(directive).toBeTruthy();
  });
});
